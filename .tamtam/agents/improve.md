---
model: normal
schedule: 12h
skillIds: ["agent-improve"]
prerequisiteCommand: "echo '## Next 5 unaudited candidates (oldest commit first, current content)'; mkdir -p .tamtam/cache/audits; : >> .tamtam/cache/audits/improve-ledger.txt; git log --format='@%ct' --name-only --no-renames 2>/dev/null | awk '/^@/{t=substr($0,2);next} NF&&!seen[$0]++{age[$0]=t} END{for(p in age)print age[p]\"\\t\"p}' > .tamtam/cache/audits/.improve-age.tmp; git ls-files 2>/dev/null | while IFS= read -r path; do case \"$path\" in *.ts|*.tsx|*.js|*.jsx|*.sol|*.py|*.rs|*.go|*.md|*.sh) ;; *) continue;; esac; case \"$path\" in *.d.ts|*.gen.*|*.generated.*) continue;; .tamtam/*|*/.tamtam/*|node_modules/*|*/node_modules/*) continue;; *__snapshots__/*|*__fixtures__/*|*/fixtures/*|*/test-results/*|*/playwright-report/*|*/coverage/*|*/dist/*|*/build/*|*/out/*) continue;; docs/superpowers/plans/*|docs/superpowers/specs/*) continue;; CHANGELOG.md|LICENSE|LICENSE.md|LICENCE|LICENCE.md) continue;; esac; sha=$(git hash-object -- \"$path\" 2>/dev/null) || continue; [ -n \"$sha\" ] || continue; grep -qxF \"$sha\" .tamtam/cache/audits/improve-ledger.txt 2>/dev/null && continue; printf '%s\\t%s\\n' \"$sha\" \"$path\"; done > .tamtam/cache/audits/.improve-cand.tmp; git ls-files --others --exclude-standard 2>/dev/null | while IFS= read -r path; do case \"$path\" in *.ts|*.tsx|*.js|*.jsx|*.sol|*.py|*.rs|*.go|*.md|*.sh) ;; *) continue;; esac; case \"$path\" in *.d.ts|*.gen.*|*.generated.*) continue;; .tamtam/*|*/.tamtam/*|node_modules/*|*/node_modules/*) continue;; *__snapshots__/*|*__fixtures__/*|*/fixtures/*|*/test-results/*|*/playwright-report/*|*/coverage/*|*/dist/*|*/build/*|*/out/*) continue;; docs/superpowers/plans/*|docs/superpowers/specs/*) continue;; CHANGELOG.md|LICENSE|LICENSE.md|LICENCE|LICENCE.md) continue;; esac; sha=$(git hash-object -- \"$path\" 2>/dev/null) || continue; [ -n \"$sha\" ] || continue; grep -qxF \"$sha\" .tamtam/cache/audits/improve-ledger.txt 2>/dev/null && continue; printf '%s\\t%s\\n' \"$sha\" \"$path\"; done >> .tamtam/cache/audits/.improve-cand.tmp; out=$(awk -F'\\t' 'BEGIN{first=ARGV[1]} FILENAME==first{age[$2]=$1;next}{a=age[$2];if(a==\"\")a=0;print a\"\\t\"$2\"\\t\"$1}' .tamtam/cache/audits/.improve-age.tmp .tamtam/cache/audits/.improve-cand.tmp | sort -n | head -5 | awk -F'\\t' '{printf \"%s  (blob %s)\\n\",$2,$3}'); rm -f .tamtam/cache/audits/.improve-age.tmp .tamtam/cache/audits/.improve-cand.tmp; if [ -n \"$out\" ]; then printf '%s\\n' \"$out\"; else echo '(all tracked and non-ignored untracked files audited at current content — idle until a file changes)'; fi; echo; echo '## Recent improve runs (tail of .tamtam/cache/audits/improve.md)'; tail -10 .tamtam/cache/audits/improve.md 2>/dev/null || echo '(no audit log yet)'"
---


